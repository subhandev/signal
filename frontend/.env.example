# Backend API (used for SSR and Next.js rewrites). Browser calls same-origin /api/* proxied to this URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_API_URL=https://signal-production-1155.up.railway.app
